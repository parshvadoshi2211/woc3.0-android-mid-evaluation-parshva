<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mid_evaluation" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mid_evaluation" >
        <activity android:name=".satActivity" android:parentActivityName=".MainActivity" >
        </activity>
        <activity android:name=".friActivity" android:parentActivityName=".MainActivity"  />
        <activity android:name=".thuActivity" android:parentActivityName=".MainActivity" />
        <activity android:name=".wedActivity"  android:parentActivityName=".MainActivity" />
        <activity android:name=".tueActivity"  android:parentActivityName=".MainActivity" />
        <activity android:name=".monActivity" android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>